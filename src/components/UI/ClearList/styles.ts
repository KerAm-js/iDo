import { StyleSheet } from "react-native";

export const clearListStyles = StyleSheet.create({
  container: {
    alignItems: 'center'
  },
  image: {
    marginTop: 30,
  },
  title: {
    marginTop: 25,
    maxWidth: '80%',
    textAlign: 'center',
  }
})