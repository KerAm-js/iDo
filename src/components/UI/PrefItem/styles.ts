import { StyleSheet } from "react-native";

export const prefItemStyles = StyleSheet.create({
  container: {
    minHeight: 50,
    marginBottom: 10,
  },
  leftItemsContainer: {
    flexDirection: 'row',
  },
  icon: {
    marginRight: 15
  }
})