import { StyleSheet, ViewStyle } from "react-native";

export const iconButtonStyle: ViewStyle = {
  minWidth: 30,
  minHeight: 30,
};

export const calendarIconButtonStyles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
  },
});
